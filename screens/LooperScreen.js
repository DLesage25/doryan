import React, { useEffect, useState } from 'react';
import {
    ScrollView,
    View,
    Text,
    StyleSheet,
    Image,
    Slider,
} from 'react-native';

const LooperScreen = () => {
    return (
        <View>
            <Text> LooperScreen </Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default LooperScreen;
